#include <Arduino.h>

void activate()
{
    interrupts();
    attachInterrupt(digitalPintoInterrupt(3),PS_ISR,CHANGE);
    attachInterrupt(UDR0,PS_ISR,CHANGE);
}

PS_ISR
{
  int PS = digitalRead(3);

  Serial.write(PS);
}

//UART interrupt??